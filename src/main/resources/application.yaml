spring:
  application:
    name: SpringLudotheque

  #Connection to DB
  datasource:
    url: jdbc:sqlserver://localhost:1433;databasename=LUDO_BD;integratedSecurity=false;encrypt=false;trustServerCertificate=false
    username: sa
    password: Pa$$w0rd

  #Options to DB
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: create